git init 

git clone 从远程仓库克隆

git add *

git commit 

git push origin master

//查看本地分支
git branch

// 查看远端的所有分支
git branch -r

// 本地新建一个分支并且进入
git checkout -b xxxx

//分支推到远程
git push origin 本地分支：远程分支

// 创建子分支
git checkout -b 新分支名 老分支名（这种写法的意思为基于老分支创建新的分支）
